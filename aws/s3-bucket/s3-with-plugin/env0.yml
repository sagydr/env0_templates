version: 2
deploy:
  steps:
    terraformPlan:
      after:
        - name: SagPlugin
          use: https://github.com/sagydr/env0-sag-plugin
          inputs:
            plan: `cat $ENV0_TF_PLAN_JSON`
            plan_path: $ENV0_TF_PLAN_JSON
