version: 2
deploy:
  steps:
    terraformPlan:
      before:
        - name: tf graph
          run: |
            npm install graphviz
            npm install dot
            npm -S i dot-language-support
            terraform graph -type=plan | dot -Tpng > graph.png
            base64 -i graph.png >> $ENV0_STEP_CONTENT