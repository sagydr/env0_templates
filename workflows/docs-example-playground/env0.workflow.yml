environments:
  vpc:
    name: 'VPC and Network'
    templateName: 'null'
  db:
    name: DB
    templateName: 'null'
    needs:
      - vpc
  eks:
    name: EKS
    templateName: 'null'
    needs:
      - vpc
      - blabla
settings:
  environmentRemovalStrategy: destroy
